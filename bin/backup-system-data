#!/bin/sh

PKG_BACKUP_FILE="$HOME/$(hostname)-packages.tar.gz"
PKG_DATA_FILE="dpkg.txt"

dpkg -l > $PKG_DATA_FILE

tar --create --gzip --verbose --file=$PKG_BACKUP_FILE /etc/apt $PKG_DATA_FILE

rm $PKG_DATA_FILE
